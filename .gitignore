# Dependencies
node_modules/

# Build output
dist/

# Runtime data (logs, tasks, sessions, memory) — but keep the project agent prompt
.bolt/
!.bolt/AGENT.md

# Environment
.env
.env.local

# Coverage
coverage/

# OS
.DS_Store

# Claude Code — personal settings (project settings.json is committed)
.claude/settings.local.json
